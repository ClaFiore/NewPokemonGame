<h3 class="gameover"> GAME OVER 
<% if @battle.user.hp == 0  %>
<h3 class="gameover"><%= button_to "Start New Game", pokemon_path(@battle.user), method: :delete %></h3>
<% else %>
<h3 class="gameover"><%= button_to "Start New Game", pokemon_path(@battle.opponent), method: :delete %></h3>
<% end %>
</h3>